import { Injectable } from '@angular/core';

@Injectable({providedIn: 'root'})
export class IconsService
{
    constructor()
    {
        // Icon registration is handled by FuseIconComponent via SVG sprite references.
    }
}
