import { IBaseModel } from 'app/core/services/base.model'

export interface I<%= classify(name) %> extends IBaseModel {
}

export interface I<%= classify(name) %>Create {
}

export interface I<%= classify(name) %>Update {
}
